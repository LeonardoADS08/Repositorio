#include <bits/stdc++.h>
using namespace std;
int main()
{
	string a;
	while (getline(cin, a) && a.length() != 0)
    {
        for (int i = 0; i < a.length(); ++i)
        {
            cout << (int)a[i] << " ";
        }
        cout << endl;
    }
    return 0;
}
